machine:
  environment:
    CIRCLE_ENV: test
    DATABASE_URL: postgres://ubuntu:@127.0.0.1:5432/circle_test

dependencies:
    cache_directories:
        - ~/.yarn-cache
        - node_modules
        - vendor
    pre:
      - yarn install

test:
    override:
        - yarn run lint
        - yarn run typecheck || true
